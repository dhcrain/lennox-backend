[project]
name = "lennox-backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "lennoxs30api>=0.2.24",
    "pyyaml>=6.0.3",
    "uvicorn[standard]>=0.52.4; platform_machine != 'armv7l'",
    "uvicorn>=0.52.4; platform_machine == 'armv7l'",
    "websockets>=15; platform_machine == 'armv7l'",
]
